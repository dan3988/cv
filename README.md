# cv

Generates a CV web page from a JSON file using Svelte.

## Build

Run `npm run build` to generate the site to the `build` folder, using the values specified in [`src/lib/data.json`](src/lib/data.json)